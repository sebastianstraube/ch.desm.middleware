<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>ch.desm.middleware</groupId>
	<artifactId>middleware-ear</artifactId>
	<packaging>ear</packaging>
	<version>0.1</version>
	<name>${project.artifactId}</name>

    <properties>
        <!-- Encoding -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Version -->
        <plugin.maven-compiler-plugin.version>3.1</plugin.maven-compiler-plugin.version>
        <jdk.version>1.7</jdk.version>

        <!-- Glassfish -->
        <local.glassfish.domain>desm</local.glassfish.domain>
        <local.glassfish.domain.username>admin</local.glassfish.domain.username>
        <local.glassfish.passfile>${local.glassfish.home}/glassfish/domains/${local.glassfish.domain}/master-password</local.glassfish.passfile>
        <local.glassfish.test.skip>true</local.glassfish.test.skip>
    </properties>

    <dependencies>
        <!-- Test -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.1</version>
            <scope>test</scope>
        </dependency>

        <!-- Serial Interface -->
        <dependency>
            <groupId>org.scream3r</groupId>
            <artifactId>jssc</artifactId>
            <version>2.8.0</version>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.17</version>
        </dependency>

        <!-- Java EE Standard -->
        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-web-api</artifactId>
            <version>7.0</version>
        </dependency>
        <dependency>
            <groupId>javax.websocket</groupId>
            <artifactId>javax.websocket-api</artifactId>
            <version>1.1</version>
        </dependency>

        <!-- maven plugin bug -->
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-project</artifactId>
            <version>3.0-alpha-2</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>3.0.4</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-artifact</artifactId>
            <version>3.0.4</version>
            <scope>provided</scope>
        </dependency>

        <!-- Project -->
        <dependency>
            <groupId>ch.desm.middleware</groupId>
            <artifactId>app-client</artifactId>
            <version>0.1</version>
            <type>app-client</type>
        </dependency>
        <dependency>
            <groupId>ch.desm.middleware</groupId>
            <artifactId>webapp</artifactId>
            <version>0.1</version>
            <type>war</type>
        </dependency>
    </dependencies>

	<build>
        <plugins>
            <plugin>
				<groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-ear-plugin</artifactId>
                <version>2.9.1</version>
                <configuration>
					<outputDirectory>${local.working.home}/${local.working.dir}/${project.artifactId}</outputDirectory>
					<modules>
						<appClientModule>
							<groupId>ch.desm.middleware</groupId>
							<artifactId>app-client</artifactId>
							<bundleFileName>middleware-client.jar</bundleFileName>
						</appClientModule>
						<webModule>
							<groupId>ch.desm.middleware</groupId>
							<artifactId>webapp</artifactId>
							<bundleFileName>middleware-webapp.war</bundleFileName>
							<contextRoot>/</contextRoot>
						</webModule>
					</modules>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.glassfish.maven.plugin</groupId>
                <artifactId>maven-glassfish-plugin</artifactId>
                <version>2.1</version>
                <configuration>
                    <glassfishDirectory>${local.glassfish.home}/glassfish</glassfishDirectory>
                    <user>${local.glassfish.domain.username}</user>
                    <passwordFile>${local.glassfish.passfile}</passwordFile>
                    <autoCreate>true</autoCreate>
                    <debug>true</debug>
                    <echo>true</echo>
                    <terse>true</terse>
                    <skip>${local.glassfish.test.skip}</skip>
                    <domain>
                        <name>${local.glassfish.domain}</name>
                        <adminPort>4848</adminPort>
                        <httpPort>8080</httpPort>
                        <httpsPort>8443</httpsPort>
                        <iiopPort>3700</iiopPort>
                        <jmsPort>7676</jmsPort>
                        <reuse>false</reuse>
                    </domain>
                    <components>
                        <component>
                            <name>middleware-ear</name>
                            <artifact>C:/Projekte/DESM-Middleware/AppSource/middleware-ear/middleware-ear-0.1.ear</artifact>
                        </component>
                    </components>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-acr-plugin</artifactId>
                <version>1.1</version>
                <extensions>true</extensions>
            </plugin>
        </plugins>
    </build>
</project>
